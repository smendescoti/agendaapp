import React from "react";
import { ScrollView, View, Text } from "react-native";

export default function EdicaoContatos({ navigation }) {

    return (
        <ScrollView>
            <View>
                <Text>Edição de Contatos</Text>
            </View>
        </ScrollView>
    )

}